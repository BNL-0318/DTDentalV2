/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LabelCategoriesDirective } from './label-categories.directive';

describe('Directive: LabelCategories', () => {
  it('should create an instance', () => {
    const directive = new LabelCategoriesDirective();
    expect(directive).toBeTruthy();
  });
});
