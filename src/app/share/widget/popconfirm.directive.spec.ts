import { PopconfirmDirective } from './popconfirm.directive';

describe('PopconfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new PopconfirmDirective();
    expect(directive).toBeTruthy();
  });
});
